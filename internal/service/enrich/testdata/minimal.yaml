openapi: 3.0.0
info:
  title: Minimal API
  version: 1.0.0
paths:
  /api/test:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    x-slsbench-unique: false
                    x-user-hint: string